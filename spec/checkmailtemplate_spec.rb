require 'spec_helper'

describe Checkmailtemplate do
  it 'has a version number' do
    expect(Checkmailtemplate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
